# worldsim-stygian-lab

Экспериментальное репо. Здесь мы исследуем процедурную генерацию миров
в стиле depth crawl — на примере «Стигийской библиотеки» (The Stygian
Library, Emmy Allen) — и пробуем перенести эти идеи в worldsim.

Это песочница: код и заметки здесь не являются частью продакшен-агентов.
Удачные находки переносятся в `worldsim-workspace` через обычный процесс.

## Что такое The Stygian Library

OSR-приключение Эммы Аллен (2018): бесконечная междумировая библиотека,
в которую можно попасть из любой достаточно большой земной библиотеки.
Партия обычно приходит туда за конкретным знанием. Карта не нарисована
заранее — библиотека генерируется по мере продвижения вглубь.

## Алгоритм depth crawl

Подробный пересказ — в [docs/stygian-algorithm.md](docs/stygian-algorithm.md).

Кратко:

1. Состояние мира — одно число `Depth` (начинается с 0) плюс растущий
   point-crawl граф уже открытых локаций.
2. Шаг вглубь: `Depth += 1`, затем броски `1d20 + Depth` по таблицам
   Locations и Details, и бросок по таблице Events (может потянуть за
   собой Encounters и Treasure).
3. Чем глубже, тем выше модификатор — верх таблиц «отваливается»,
   открываются всё более странные и опасные результаты. Глубина и есть
   сложность.
4. Открытые локации фиксируются на своей глубине; назад можно вернуться
   по уже построенному графу.
5. Цель отслеживается абстрактным счётчиком Progress: разговоры со
   знающими NPC и найденные книги двигают его к порогу искомого знания.

## Структура

```
worldsim-stygian-lab/
├── README.md
├── docs/
│   ├── stygian-algorithm.md       ← разбор алгоритма генерации
│   ├── prototype-prompt.md        ← мега-промпт: полное ТЗ на прототип одним промптом для Fable 5
│   └── design/                    ← дизайн-доки мега-промпта (механика, контент, архитектура, UI)
├── prototype/
│   └── index.html                 ← point-crawl прототип (vanilla JS + SVG), собран по мега-промпту
└── experiments/
    └── 01-depthcrawl-canvas/      ← играбельный 2D-прототип (vanilla JS + Canvas)
        ├── index.html             ← вся игра, один файл, без зависимостей
        └── README.md              ← управление, дизайн-решения, маппинг на алгоритм
```

## Прототип: как поиграть

Открыть `experiments/01-depthcrawl-canvas/index.html` в браузере
(двойным кликом или через `python -m http.server`). WASD/стрелки —
движение, двери генерируют новые комнаты броском d20 + глубина.

## Второй прототип: point-crawl карта (`prototype/`)

Открыть `prototype/index.html` в браузере. Управление мышью: клик по
узлу-заглушке со знаком вопроса генерирует комнату (d20+глубина по
таблицам локаций и деталей, d12 на событие); открытые комнаты остаются
на карте навсегда, карта растёт вниз. Цель — счётчик Progress: выбрать
вопрос на старте, набрать порог на нужной глубине и вернуться ко входу
живым. Seed повторяет партию, автосейв в localStorage.

Этот прототип построен агентом строго по единому промпту
`docs/prototype-prompt.md` (1946 строк): файл одновременно служит
полным ТЗ и боевым тестом того, что весь мир собирается одним
промптом. Дизайн-доки, из которых промпт собран, лежат в `docs/design/`.
